// Generated by @canonical/summon-component:react v0.1.0

import { renderToString } from "react-dom/server";
import { describe, expect, it } from "vitest";
import StandardReadingPage from "./StandardReadingPage.js";

describe("StandardReadingPage SSR", () => {
  it("renders without hydration errors", () => {
    const html = renderToString(
      <StandardReadingPage>Test content</StandardReadingPage>,
    );
    expect(html).toContain("Test content");
    expect(html).toContain("ds standard-reading-page");
  });
});
