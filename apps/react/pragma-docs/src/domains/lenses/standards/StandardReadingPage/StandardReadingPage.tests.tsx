// Generated by @canonical/summon-component:react v0.1.0

import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import StandardReadingPage from "./StandardReadingPage.js";

describe("StandardReadingPage", () => {
  it("renders children", () => {
    render(<StandardReadingPage>Test content</StandardReadingPage>);
    expect(screen.getByText("Test content")).toBeInTheDocument();
  });

  it("applies custom className", () => {
    render(
      <StandardReadingPage className="custom-class">
        Content
      </StandardReadingPage>,
    );
    const element = screen.getByText("Content");
    expect(element.className).toContain("ds standard-reading-page");
    expect(element.className).toContain("custom-class");
  });

  it("passes through additional props", () => {
    render(
      <StandardReadingPage data-testid="test-component">
        Content
      </StandardReadingPage>,
    );
    expect(screen.getByTestId("test-component")).toBeInTheDocument();
  });
});
