// Generated by @canonical/summon-component:react v0.1.0

import type React from "react";
import type { StandardReadingPageProps } from "./types.js";
import "./styles.css";

const componentCssClassName = "ds standard-reading-page";

/**
 * StandardReadingPage component
 */
const StandardReadingPage = ({
  className,
  children,
  ...props
}: StandardReadingPageProps): React.ReactElement => {
  return (
    <div
      className={[componentCssClassName, className].filter(Boolean).join(" ")}
      {...props}
    >
      {children}
    </div>
  );
};

export default StandardReadingPage;
