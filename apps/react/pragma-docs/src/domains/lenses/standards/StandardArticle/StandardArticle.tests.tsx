// Generated by @canonical/summon-component:react v0.1.0

import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import StandardArticle from "./StandardArticle.js";

describe("StandardArticle", () => {
  it("renders children", () => {
    render(<StandardArticle>Test content</StandardArticle>);
    expect(screen.getByText("Test content")).toBeInTheDocument();
  });

  it("applies custom className", () => {
    render(<StandardArticle className="custom-class">Content</StandardArticle>);
    const element = screen.getByText("Content");
    expect(element.className).toContain("ds standard-article");
    expect(element.className).toContain("custom-class");
  });

  it("passes through additional props", () => {
    render(
      <StandardArticle data-testid="test-component">Content</StandardArticle>,
    );
    expect(screen.getByTestId("test-component")).toBeInTheDocument();
  });
});
