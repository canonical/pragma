// Generated by @canonical/summon-component:react v0.1.0

import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import StandardsIndex from "./StandardsIndex.js";

describe("StandardsIndex", () => {
  it("renders children", () => {
    render(<StandardsIndex>Test content</StandardsIndex>);
    expect(screen.getByText("Test content")).toBeInTheDocument();
  });

  it("applies custom className", () => {
    render(<StandardsIndex className="custom-class">Content</StandardsIndex>);
    const element = screen.getByText("Content");
    expect(element.className).toContain("ds standards-index");
    expect(element.className).toContain("custom-class");
  });

  it("passes through additional props", () => {
    render(
      <StandardsIndex data-testid="test-component">Content</StandardsIndex>,
    );
    expect(screen.getByTestId("test-component")).toBeInTheDocument();
  });
});
