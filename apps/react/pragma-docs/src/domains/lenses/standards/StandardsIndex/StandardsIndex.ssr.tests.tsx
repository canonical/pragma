// Generated by @canonical/summon-component:react v0.1.0

import { renderToString } from "react-dom/server";
import { describe, expect, it } from "vitest";
import StandardsIndex from "./StandardsIndex.js";

describe("StandardsIndex SSR", () => {
  it("renders without hydration errors", () => {
    const html = renderToString(<StandardsIndex>Test content</StandardsIndex>);
    expect(html).toContain("Test content");
    expect(html).toContain("ds standards-index");
  });
});
