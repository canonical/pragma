// Generated by @canonical/summon-component:react v0.1.0

import { describe, expect, it } from "vitest";
import { renderToString } from "react-dom/server";
import LensBreadcrumbs from "./LensBreadcrumbs.js";

describe("LensBreadcrumbs SSR", () => {
	it("renders without hydration errors", () => {
		const html = renderToString(<LensBreadcrumbs>Test content</LensBreadcrumbs>);
		expect(html).toContain("Test content");
		expect(html).toContain("ds lens-breadcrumbs");
	});
});
