/** The canvas element's stable structural identity: the skip link's target,
 * and the marker `frameStability.tests.tsx` extracts the frame around. */
export const CANVAS_ELEMENT_ID = "shell-canvas";

/** The `meta` key under which a route's composed layout claims the mode
 * strip's sockets (the P-5 handshake — see `shellStripFacet`). Exported
 * because it names the key in tests and in the claim sites' prose; the
 * claims themselves author through the facet, not through this constant. */
export const SHELL_STRIP_META_KEY = "shellStrip";
