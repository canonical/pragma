name: "Pull request"
on:
  pull_request:

env:
  NX_SELF_HOSTED_REMOTE_CACHE_SERVER: "http://localhost:9876"
  NX_SELF_HOSTED_REMOTE_CACHE_ACCESS_TOKEN: "local"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        bun-version: ['1.3.9']
        # Node 20 EOL is April 2026. Testing on 22 (LTS) and 24 (current).
        node-version: ['22', '24']
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup environment
        uses: ./.github/actions/setup-env
        with:
          bun-version: ${{ matrix.bun-version }}
          node-version: ${{ matrix.node-version }}

      - name: Build affected packages
        run: bunx nx affected -t build:all --base=origin/main

  check:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        bun-version: ['1.3.9']
        node-version: ['22', '24']
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup environment
        uses: ./.github/actions/setup-env
        with:
          bun-version: ${{ matrix.bun-version }}
          node-version: ${{ matrix.node-version }}

      - name: Build affected packages
        run: bunx nx affected -t build:all --base=origin/main

      - name: Code quality checks
        run: bunx nx affected -t check --base=origin/main

  test:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        bun-version: ['1.3.9']
        node-version: ['22', '24']
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup environment
        uses: ./.github/actions/setup-env
        with:
          bun-version: ${{ matrix.bun-version }}
          node-version: ${{ matrix.node-version }}

      - name: Build affected packages
        run: bunx nx affected -t build:all --base=origin/main

      - name: Test
        run: bunx nx affected -t test --base=origin/main

  build-gate:
    runs-on: ubuntu-latest
    needs: [check, test]
    if: ${{ success() }}
    steps:
      - name: All checks passed
        run: echo "All build matrix jobs completed successfully"
