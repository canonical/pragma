name: "Pull request"
on:
  pull_request:

jobs:
  build-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        bun-version: ['1.2.19']
        # Node 21 is EOL and unsupported by jsdom 27.x. Storybook Node 24 issues don't affect this workflow.
        node-version: ['20', '22', '24']
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Setup environment for Bun ${{ matrix.bun-version }} and Node ${{ matrix.node-version }}
        uses: ./.github/actions/setup-env
        with:
          bun-version: ${{ matrix.bun-version }}
          node-version: ${{ matrix.node-version }}

      - name: Build all artifacts
        run: lerna run build:all

      - name: Code quality checks
        run: bun run check

      - name: Test
        run: bun run test
  build-gate:
    runs-on: ubuntu-latest
    needs: build-matrix
    if: ${{ success() }}
    steps:
      - name: All checks passed
        run: echo "All build matrix jobs completed successfully"