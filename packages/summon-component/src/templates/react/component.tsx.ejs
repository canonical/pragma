// Generated by <%= generatorName %> v<%= version %>

import type { <%= name %>Props } from "./types";
<% if (withStyles) { -%>
import "./styles.css";
<% } -%>

/**
 * <%= name %> component
 */
export const <%= name %> = ({
	className,
	children,
	...props
}: <%= name %>Props): JSX.Element => {
	return (
		<div
			className={`<%= kebabName %>${className ? ` ${className}` : ""}`}
			{...props}
		>
			{children}
		</div>
	);
};
