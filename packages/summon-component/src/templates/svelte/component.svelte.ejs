<!-- Generated by <%= generatorName %> v<%= version %> -->

<script lang="ts">
	import type { <%= name %>Props } from "./types";
<% if (withStyles) { -%>
	import "./styles.css";
<% } -%>

	let {
		class: className = "",
		children,
		...props
	}: <%= name %>Props = $props();
</script>

<div
	class="ds <%= kebabName %>{className ? ` ${className}` : ''}"
	{...props}
>
	{@render children?.()}
</div>
