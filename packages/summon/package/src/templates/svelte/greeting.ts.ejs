/**
 * Build this package's sample greeting.
 *
 * A plain module, so the scaffold has something for the `server` test project
 * to cover alongside the component's SSR and client tests.
 */
export function greeting(name: string): string {
  return `Hello from ${name}`;
}
