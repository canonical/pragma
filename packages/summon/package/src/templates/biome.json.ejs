{
  "extends": ["@canonical/biome-config"],
  "files": {
    "includes": ["src", "*.json", "*.ts"<% if (framework === 'svelte') { %>, "*.js"<% } %><% if (withStorybook) { %>, ".storybook"<% } %>]
  }
}
