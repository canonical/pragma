/**
 * <%= name %>
 *
 * <%= description || 'TODO: Add description' %>
 */

/**
 * This package's own name.
 *
 * A placeholder so the scaffold ships one real export and one real test —
 * replace it with this package's API.
 */
export const PACKAGE_NAME = "<%= name %>";
