/* Generated by <%= generatorName %> v<%= version %> */

:host {
  /*
   * Host controls the outer layout role.
   * Declare component CSS variables here when needed.
   * Variables must be on :host to be accessible inside the shadow tree.
   *
   * Example:
   * --<%= kebabName %>-color-background: var(--color-background-default);
   * --<%= kebabName %>-color-text: var(--color-text-default);
   */
  display: inline-flex; /* or block for full-width, contents for structural wrappers */
}

.ds.<%= kebabName %> {
  /* Inner layout and visual styles go here. */
}

/*
 * Grid guidance:
 * - If this component defines layout, use `display: grid` on `.ds.<%= kebabName %>`.
 * - If this component consumes a parent grid, use `grid-column`/`grid-row` on `:host`.
 * - For subgrid usage, also set `display: grid` and `grid-template-columns: subgrid`.
 */
