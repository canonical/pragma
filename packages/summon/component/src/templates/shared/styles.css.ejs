/* Layered so an app tier's rule wins by cascade layer, not by load order.
 * See "Cascade layers" in the @canonical/styles README. */
@layer ds.components.global {
  .ds.<%= kebabName %> {
    /* Component styles */
  }
}
