/**
 * The published package name, used in generator `displayName` labels and the
 * version walk-up. A constant rather than a `package.json` JSON import: the
 * import path would resolve differently from `src/` and `dist/esm/`.
 */
export const PACKAGE_NAME = "@canonical/summon-application";
