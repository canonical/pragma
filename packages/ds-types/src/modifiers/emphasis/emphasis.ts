/**
 * A list of emphasis modifiers.
 * These modifiers can be used to change the emphasis of an element.
 * For a type definition, see {@link Emphasis}.
 */
const EMPHASIS = ["muted", "highlighted"] as const;

export default EMPHASIS;
