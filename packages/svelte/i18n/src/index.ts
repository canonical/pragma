/**
 * @canonical/i18n-svelte — Svelte bindings for @canonical/i18n-core: `locale`,
 * `direction`, `t`, and `formatters` stores derived from a shared locale source,
 * which already satisfies the Svelte store contract.
 *
 * @packageDocumentation
 */

export * from "./lib/index.js";
