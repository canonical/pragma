<!-- Generated by @canonical/summon-component:svelte v0.1.0 -->

<script lang="ts" module>
  import { MODIFIER_FAMILIES } from "@canonical/ds-types";
  import { defineMeta } from "@storybook/addon-svelte-csf";
  import Announcement from "./Announcement.svelte";

  const { Story } = defineMeta({
    title: "_work_in_progress/Components/Announcement",
    component: Announcement,
    tags: ["autodocs"],
    argTypes: {
      criticality: {
        control: { type: "radio" },
        options: [undefined, ...MODIFIER_FAMILIES.criticality],
      },
    },
    parameters: {
      docs: {
        description: {
          component:
            "The announcement is a persistent, high-visibility UI block used to communicate time-sensitive or critical contextual information directly within a page. It is not triggered by specific system or user events; instead, it is a non-transient call-out used for broad organisational updates, compliance warnings, or upcoming deadlines.",
        },
      },
    },
  });
</script>

<Story
  name="Default"
  args={{
    criticality: "information",
    heading: "System Maintenance",
  }}
>
  Maintenance is scheduled for tonight, Nov 12, from 11 PM - 2 AM PST. Some services may be unavailable.
</Story>

<Story
  name="WithoutHeading"
  args={{
    criticality: "information",
  }}
  parameters={{
    docs: {
      description: {
        story: "The heading is optional — an announcement may be content-only.",
      },
    },
  }}
>
  Maintenance is scheduled for tonight, Nov 12, from 11 PM - 2 AM PST.
</Story>

<Story
  name="Success"
  args={{
    criticality: "success",
    heading: "Deployment complete",
  }}
>
  Your changes have been successfully deployed to production.
</Story>

<Story
  name="Warning"
  args={{
    criticality: "warning",
    heading: "Approaching quota limit",
  }}
>
  You have used 90% of your monthly API quota. Consider upgrading your plan.
</Story>

<Story
  name="Error"
  args={{
    criticality: "error",
    heading: "Service disruption",
  }}
>
  We are experiencing issues with the authentication service. Our team is investigating.
</Story>

<Story
  name="WithAlertRole"
  args={{
    criticality: "error",
    heading: "Connection lost",
    role: "alert",
    "aria-live": "assertive",
  }}
  parameters={{
    docs: {
      canvas: {
        sourceState: "shown",
      },
      description: {
        story: `\`criticality\` only controls the announcement's visual treatment.  
It doesn't imply an ARIA role or live-region behavior.

Per [MDN's guidance on the \`alert\` role](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Roles/alert_role),
\`role="alert"\` should be reserved for content that demands the user's
immediate attention, and used
sparingly. A persistently-rendered \`criticality="error"\` announcement, for
example, is not necessarily an alert.
The right semantics depend on how
the announcement is used rather than how it looks. 

Consumers should set
\`role\` and \`aria-live\` explicitly when appropriate, as in the example below.`,
      },
    },
  }}
>
  Local changes will not be saved until the connection is restored.
</Story>

<Story
  name="LongMessage"
  args={{
    criticality: "information",
    heading: "Scheduled maintenance and service migration",
  }}
  parameters={{
    docs: {
      description: {
        story:
          "A longer body wraps across multiple lines; the icon stays top-aligned and the text column flows beneath the heading.",
      },
    },
  }}
>
  Over the coming weekend we will migrate several backend services to a new region to improve latency and resilience. During the migration window — Saturday 22:00 through Sunday 06:00 UTC — dashboards may load slowly, exports may be delayed, and some automated jobs will be queued rather than run immediately. No data will be lost, and any queued work will complete once the migration finishes.
</Story>

<Story
  name="RightToLeft"
  args={{
    criticality: "information",
    heading: "صيانة النظام",
  }}
  parameters={{
    docs: {
      description: {
        story:
          "Right-to-left layout (Arabic). Logical properties flip the side bar to the trailing (right) edge and the icon leads on the right.",
      },
    },
  }}
  asChild
>
  <div dir="rtl">
    <Announcement criticality="information" heading="صيانة النظام">
      من المقرر إجراء صيانة الليلة، 12 نوفمبر، من الساعة 11 مساءً حتى 2 صباحًا بتوقيت المحيط الهادئ. قد تكون بعض الخدمات غير متوفرة.
    </Announcement>
  </div>
</Story>
