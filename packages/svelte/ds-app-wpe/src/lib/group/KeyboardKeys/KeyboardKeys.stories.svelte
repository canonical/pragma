<!-- Generated by @canonical/summon-component:svelte v0.1.0 -->

<script lang="ts" module>
  import { defineMeta } from "@storybook/addon-svelte-csf";
  import { KeyboardKey } from "../../components/KeyboardKey/index.js";
  import KeyboardKeys from "./KeyboardKeys.svelte";

  const { Story } = defineMeta({
    title: "Groups/KeyboardKeys",
    component: KeyboardKeys,
    tags: ["autodocs"],
    parameters: {
      // Svelte 5 Storybook autodocs do not currently pick up component docs from
      // the component source reliably, so we inject the description here instead:
      // https://github.com/storybookjs/storybook/discussions/34104#discussioncomment-16084065
      docs: {
        description: {
          component:
            'KeyboardKeys groups two or more KeyboardKey components into a single inline unit with consistent spacing. Use it to represent multi-key shortcuts (e.g., Ctrl + C) or key sets (e.g., arrow keys) as a visually cohesive group. It accepts arbitrary children, including separator text, alongside KeyboardKey instances.\n\nThis component renders a `<kbd>` element and is designed to contain other `<kbd>` elements (e.g., individual keys) as children, forming a single grouped keystroke.\n\n`import { KeyboardKeys } from "@canonical/svelte-ds-app-wpe";`',
        },
      },
    },
  });
</script>

<Story name="Default">
  {#snippet template()}
    <KeyboardKeys>
      <KeyboardKey keyValue="up" />
      <KeyboardKey keyValue="right" />
      <KeyboardKey keyValue="down" />
      <KeyboardKey keyValue="left" />
    </KeyboardKeys>
  {/snippet}
</Story>

<Story name="ComposedCommand">
  {#snippet template()}
    <p class="p">
      Press
      <KeyboardKeys>
        <KeyboardKey keyValue="ctrl" />
        <KeyboardKey keyValue="c" />
      </KeyboardKeys>
      to copy.
    </p>
  {/snippet}
</Story>
