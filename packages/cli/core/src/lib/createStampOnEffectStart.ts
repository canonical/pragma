/**
 * Re-export shim: `createStampOnEffectStart` moved to `@canonical/summon-core`
 * (THE stamping path). Removed with cli-core in PR8.
 */

export { createStampOnEffectStart as default } from "@canonical/summon-core";
