/**
 * @canonical/i18n-lit — Lit bindings for @canonical/i18n-core: a reactive
 * `LocaleController` that re-renders a host element on locale change and exposes
 * the translator and formatters for the active locale.
 *
 * @packageDocumentation
 */

export * from "./lib/index.js";
