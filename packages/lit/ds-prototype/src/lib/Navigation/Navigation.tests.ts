// Generated by @canonical/summon-component:lit v0.1.0

import { afterEach, beforeEach, describe, expect, it } from "vitest";
import "./Navigation.js";
import type Navigation from "./Navigation.js";

describe("Navigation component", () => {
  let elem: Navigation;

  beforeEach(() => {
    elem = document.createElement("ds-navigation") as Navigation;
    document.body.appendChild(elem);
  });

  afterEach(() => {
    elem.remove();
  });

  it("should render component", async () => {
    await customElements.whenDefined("ds-navigation");

    const container = elem.shadowRoot?.querySelector(".ds.navigation");
    expect(container).toBeTruthy();
  });

  it("should have correct tag name", () => {
    expect(elem.tagName.toLowerCase()).toBe("ds-navigation");
  });
});
