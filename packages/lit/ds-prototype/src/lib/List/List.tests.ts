// Generated by @canonical/summon-component:lit v0.1.0

import { afterEach, beforeEach, describe, expect, it } from "vitest";
import "./List.js";
import type List from "./List.js";

describe("List component", () => {
  let elem: List;

  beforeEach(() => {
    elem = document.createElement("ds-list") as List;
    document.body.appendChild(elem);
  });

  afterEach(() => {
    elem.remove();
  });

  it("should render component", async () => {
    await customElements.whenDefined("ds-list");

    const container = elem.shadowRoot?.querySelector(".ds.list");
    expect(container).toBeTruthy();
  });

  it("should have correct tag name", () => {
    expect(elem.tagName.toLowerCase()).toBe("ds-list");
  });
});
