// Generated by @canonical/summon-component:lit v0.1.0

import { afterEach, beforeEach, describe, expect, it } from "vitest";
import "./SiteLayout.js";
import type SiteLayout from "./SiteLayout.js";

describe("SiteLayout component", () => {
  let elem: SiteLayout;

  beforeEach(() => {
    elem = document.createElement("ds-site-layout") as SiteLayout;
    document.body.appendChild(elem);
  });

  afterEach(() => {
    elem.remove();
  });

  it("should render component", async () => {
    await customElements.whenDefined("ds-site-layout");

    const container = elem.shadowRoot?.querySelector(".ds.site-layout");
    expect(container).toBeTruthy();
  });

  it("should have correct tag name", () => {
    expect(elem.tagName.toLowerCase()).toBe("ds-site-layout");
  });
});
