/* <%= generatorPackageName %> <%= generatorPackageVersion %> */

.<%= cssNamespace %>.<%= componentCssClassName %> {
    /** a comment that explains the purpose of this variable */
    --<%= componentCssClassName.toLowerCase() %>-color-text: black;

    color: var(--<%= componentCssClassName.toLowerCase() %>-color-text);
}