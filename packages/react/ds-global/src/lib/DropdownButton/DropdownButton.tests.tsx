// Generated by @canonical/summon-component:react v0.1.0

import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import DropdownButton from "./DropdownButton.js";

describe("DropdownButton", () => {
	it("renders children", () => {
		render(<DropdownButton>Test content</DropdownButton>);
		expect(screen.getByText("Test content")).toBeInTheDocument();
	});

	it("applies custom className", () => {
		render(<DropdownButton className="custom-class">Content</DropdownButton>);
		const element = screen.getByText("Content");
		expect(element.className).toContain("ds dropdown-button");
		expect(element.className).toContain("custom-class");
	});

	it("passes through additional props", () => {
		render(<DropdownButton data-testid="test-component">Content</DropdownButton>);
		expect(screen.getByTestId("test-component")).toBeInTheDocument();
	});
});
