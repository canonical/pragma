// Generated by @canonical/summon-component:react v0.1.0

import { describe, expect, it } from "vitest";
import { render, screen } from "@testing-library/react";
import SideNavigation from "./SideNavigation.js";

describe("SideNavigation", () => {
	it("renders children", () => {
		render(<SideNavigation>Test content</SideNavigation>);
		expect(screen.getByText("Test content")).toBeInTheDocument();
	});

	it("applies custom className", () => {
		render(<SideNavigation className="custom-class">Content</SideNavigation>);
		const element = screen.getByText("Content");
		expect(element.className).toContain("ds side-navigation");
		expect(element.className).toContain("custom-class");
	});

	it("passes through additional props", () => {
		render(<SideNavigation data-testid="test-component">Content</SideNavigation>);
		expect(screen.getByTestId("test-component")).toBeInTheDocument();
	});
});
