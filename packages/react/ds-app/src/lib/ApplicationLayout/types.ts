// Generated by @canonical/summon-component:react v0.1.0

import type { HTMLAttributes, ReactNode } from "react";

export interface ApplicationLayoutProps extends HTMLAttributes<HTMLDivElement> {
  /** Main content (default slot). Typically a ViewLayout. */
  children?: ReactNode;
  /**
   * Application navigation (named slot), rendered in the leading
   * `min-content` column. Slot-agnostic by design: pass any ReactNode
   * (e.g. a SideNavigation) — the layout does not import or assume one.
   */
  navigation?: ReactNode;
}
