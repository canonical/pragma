// Generated by @canonical/summon-component:react v0.1.0

import type { ModifierFamily } from "@canonical/ds-types";
import type { HTMLAttributes, ReactNode } from "react";

/**
 * The grid preset laying out the content items — the `grid` modifier family
 * from \@canonical/ds-types, rendered by the `.grid` presets in
 * \@canonical/styles:
 *
 * - `responsive` — fixed-responsive, breakpoint-driven columns: 4 below
 *   768px, 8 to 1279px, 12 from 1280px.
 * - `intrinsic` — fluid auto-fill in groups of four
 *   `minmax(--grid-col-min, 1fr)` columns; column count follows available
 *   width, not breakpoints.
 */
export type ContentLayoutGrid = ModifierFamily<"grid">;

export interface ContentLayoutProps extends HTMLAttributes<HTMLDivElement> {
  /** Content items (default slot) — direct grid items. */
  children?: ReactNode;
  /** Grid preset. Defaults to `responsive` (fixed-responsive). */
  grid?: ContentLayoutGrid;
}
