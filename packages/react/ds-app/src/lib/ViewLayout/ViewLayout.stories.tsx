// Generated by @canonical/summon-component:react v0.1.0

import type { Meta, StoryObj } from "@storybook/react-vite";
import {
  LayoutSlot,
  layoutDecorators,
} from "#storybook/layouts/story-utils.js";
import ViewLayout from "./ViewLayout.js";

const meta: Meta<typeof ViewLayout> = {
  title: "Layouts/ViewLayout",
  component: ViewLayout,
  tags: ["autodocs"],
  parameters: { layout: "fullscreen" },
  decorators: layoutDecorators,
};

export default meta;
type Story = StoryObj<typeof ViewLayout>;

/** Content (1fr) + contextual aside (min-content). */
export const Default: Story = {
  args: {
    children: <LayoutSlot name="default" />,
    aside: <LayoutSlot name="aside" style={{ inlineSize: "20rem" }} />,
  },
};

/** Without the aside slot, the content takes the full width. */
export const WithoutAside: Story = {
  args: {
    children: <LayoutSlot name="default" />,
  },
};
