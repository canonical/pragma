// Generated by @canonical/summon-component:react v0.1.0

import type { HTMLAttributes, ReactNode } from "react";

export interface ViewLayoutProps extends HTMLAttributes<HTMLDivElement> {
  /** View content (default slot). Typically a ContentLayout. */
  children?: ReactNode;
  /**
   * Contextual aside (named slot), rendered in the trailing `min-content`
   * column — a detail panel, filters, help. Slot-agnostic: pass any
   * ReactNode; the slot owns its inline size.
   */
  aside?: ReactNode;
}
