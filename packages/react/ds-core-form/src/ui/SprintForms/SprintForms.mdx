## Forms API Exercise

This exercise will give you experience with various parts of the forms API.

1. Create a story that maps field props to fields. See `SprintForms/FieldMapping.tsx` for example.
2. Create a story that uses Middleware. See `middleware/middleware.stories.tsx` for example.
