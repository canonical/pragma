// Generated by @canonical/summon-component:react v0.1.0

import { forwardRef, type ReactElement } from "react";
import type { RadioInputProps } from "./types.js";
import "./styles.css";

const componentCssClassName = "ds form-radio";

/**
 * Presentational radio input — pure markup, no react-hook-form. The atomic
 * single-select indicator only; it carries no label (the host choices
 * group/field supplies the option's label, the set, and validation).
 *
 * Usable standalone (controlled via `checked`/`onChange`, or uncontrolled) or
 * via a host that spreads its props onto it.
 * @returns {ReactElement} - Rendered RadioInput
 */
export const RadioInput = forwardRef<HTMLInputElement, RadioInputProps>(
  function RadioInput(
    { id, className, style, ...nativeProps },
    ref,
  ): ReactElement {
    return (
      <input
        id={id}
        style={style}
        type="radio"
        className={[componentCssClassName, className].filter(Boolean).join(" ")}
        ref={ref}
        {...nativeProps}
      />
    );
  },
);

export default RadioInput;
