// Generated by @canonical/summon-component:react v0.1.0

import { renderToString } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { RadioInput } from "./RadioInput.js";

// Proves the presentational radio renders to static HTML with no client runtime
// / form context — the server-rendered floor for progressive enhancement.
describe("RadioInput SSR", () => {
  it("renders to static HTML without a form context", () => {
    const html = renderToString(<RadioInput name="choice" value="a" />);
    expect(html).toContain("<input");
    expect(html).toContain('type="radio"');
    expect(html).toContain('name="choice"');
    expect(html).toContain("ds form-radio");
  });
});
