import type { ComponentProps } from "react";

/** Props for the presentational Hidden input (no react-hook-form). Renders an
 * `<input type="hidden">` as its root. */
export type HiddenInputProps = ComponentProps<"input">;
