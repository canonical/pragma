import type { TextInputProps } from "#lib/subcomponent/TextInput/index.js";
import type { InputProps } from "../types.js";

/** Props for the react-hook-form-bound Text field (presentational + binding). */
export type TextProps = InputProps<TextInputProps>;
