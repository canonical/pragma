import type { TextPresentationProps } from "../../inputs/Text/index.js";
import type { InputProps } from "../types.js";

/** Props for the react-hook-form-bound Text field (presentational + binding). */
export type TextProps = InputProps<TextPresentationProps>;
