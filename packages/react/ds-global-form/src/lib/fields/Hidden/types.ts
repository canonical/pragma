import type { HiddenPresentationProps } from "../../inputs/Hidden/index.js";
import type { InputProps } from "../types.js";

/** Props for the react-hook-form-bound Hidden field. */
export type HiddenProps = InputProps<HiddenPresentationProps>;
