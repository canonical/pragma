import type { InputProps } from "../../common/types.js";
import type { TextInputProps } from "../../subcomponent/TextInput/index.js";

/** Props for the react-hook-form-bound Text field (presentational + binding). */
export type TextFieldProps = InputProps<TextInputProps>;
