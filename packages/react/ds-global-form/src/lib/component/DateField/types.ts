import type { InputProps } from "../../common/types.js";
import type { DateInputProps } from "../../subcomponent/DateInput/index.js";

/** Props for the react-hook-form-bound date field (presentational + binding). */
export type DateFieldProps = InputProps<DateInputProps>;
