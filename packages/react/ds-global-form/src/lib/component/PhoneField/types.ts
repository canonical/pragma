import type { InputProps } from "#lib/common/types.js";
import type { PhoneInputProps } from "#lib/subcomponent/PhoneInput/index.js";

/** Props for the react-hook-form-bound Phone field. */
export type PhoneFieldProps = InputProps<PhoneInputProps>;
